"use strict"; // treat all JS code as newer version
// its good habit to write this statement but newer versions run and understands without even writing this.

// alert( 3 + 3) // we are using nodejs, not browser thats why we are getting error

// semicolon is optional
console.log(3 
    +
     3) // code readability should be high

console.log("Hitesh")

// alert(3+3);
let name = "hitesh"
let age = 18
let isLoggedIn = false
let state;

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined => 
// symbol => unique


// object

console.log(typeof undefined); // undefined
console.log(typeof null); // object
